API_URL="http://localhost:3001"

# --- Supabase (OPCIONAL) ---
# O backend padrão deste frontend é o NestJS (API_URL acima). O app arranca e faz
# login sem nenhuma destas variáveis.
# Só as defina se quiser trocar o repositório de perfil para o Supabase: o
# composition root (src/composition/auth.ts) usa o SupabaseProfileRepository
# apenas quando AMBAS estiverem preenchidas.
# VITE_SUPABASE_URL="https://xxxxxxxxxxxx.supabase.co"
# VITE_SUPABASE_PUBLISHABLE_KEY=""
